fnc print(a : str, ...) : void;

trait Add {
	fnc sum(const a : Self, const b : Self) : Self;
}

struct Point {
	x : i32,
	y : i32,
}

impl Add for Point {
	fnc sum(const a: Self, const b: Self) : Self {
		return : Self = {
			x = a.x + b.x,
			y = a.y + b.y,
		};

		ret return;
	}
}

fnc main() : i32 {
	const pta : Point = {
		x = 20,
		y = 40,
	};

	const ptb : Point = {
		x = 50,
		y = 60,
	};

	ptc : Point;
	ptc = pta + ptb;
	print("x : %d, y = %d", ptc.x, ptc.y);
	ret 0;
}
